import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "@/context/AuthContext";

const geistSans = Geist({
  variable,
  subsets,
});

const geistMono = Geist_Mono({
  variable,
  subsets,
});

export const metadata= {
  title,
  description, plumbing, and appliance repair work orders",
  manifest,
  appleWebApp,
    statusBarStyle,
    title,
  },
  formatDetection,
  },
  themeColor,
  viewport,
    initialScale,
    maximumScale,
    userScalable,
  },
  icons,
    apple,
  },
};

export default function RootLayout({
  children,
}: Readonly
      
        {children}
      
    
  );
}
